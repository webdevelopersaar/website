import styled from "styled-components"
export default styled.footer`
  position: relative;
  margin-bottom: 3rem;

  ul,
  ol {
    display: flex;
    align-items: center;
    justify-content: space-around;
    flex-direction: row;
    margin: 0;
    padding: 0;
  }

  li {
    list-style: none;
    display: inline;
  }
`
